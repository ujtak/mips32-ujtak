% load "parameters.rb"
% inst_file = ""
% max_step = 256

`timescale 1ns/1ps

module test_top();
`include "parameters.vh"

  /*AUTOREGINPUT*/

  /*AUTOWIRE*/

  //clock
  always
  begin
    clk = 0;
    #(STEP/2);
    clk = 1;
    #(STEP/2);
  end

  //flow
  integer i;
  initial
  begin
    xrst = 0;
    soft_reset  = 0;
    clear_mems;
    #(STEP);

    xrst = 1;
    soft_reset = 1;
    init_mem_inst;
    #(STEP);

    soft_reset = 0;
    #(STEP);

    for (i=0; i<<%=max_step%>; i=i+1) #(STEP);

    soft_reset = 1;
    #(STEP*10);

    dump_mem_data;
    $finish();
  end

  top dut0(/*AUTOINST*/);

  task clear_mems;
    integer i;
    begin // {{{
      for (i=0; i<2**ISIZE; i=i+1)
        dut0.mem_inst.mem[i] = {DWIDTH{1'b0}};
      for (i=0; i<2**DSIZE; i=i+1)
        dut0.mem_data.mem[i] = {DWIDTH{1'b0}};
    end // }}}
  endtask

  task init_mem_inst;
    integer i;
    begin // {{{
      $readmemh("<%=inst_file%>", dut0.mem_inst.mem);
    end // }}}
  endtask

  task dump_mem_data;
    integer fd;
    integer i;
    begin // {{{
      fd = $fopen("test_top.dat", "w");
      for (i=0; i<2**DSIZE; i=i+1)
        $fdisplay(fd, "%0x", dut0.mem_data.mem[i]);
      $fclose(fd);
    end // }}}
  endtask

  //display
  always
  begin
    #(STEP/2-1);
    $display(
      "%d: ", $time/STEP,
      "| ",
    );
    #(STEP/2+1);
  end

endmodule

// Local Variables:
// verilog-library-directories:("." "..")
// End:
